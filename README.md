# Projet Quiz 

## ✅ Prérequis
- **Node.js >= 16** et **npm** installés

## 🔧 Installation
 Installer les dépendances :
   ```bash
   npm install
   ```

## ▶️ Lancer en développement
- Démarrer le serveur dev :
  ```bash
  npm run dev
  ```
- Ouvrir : `http://localhost:5173` (Vite choisira un autre port si 5173 est occupé)

## 📦 Build & Preview (production)
- Construire :
  ```bash
  npm run build
  ```
- Prévisualiser le build :
  ```bash
  npm run preview
  ```

## 💡 Remarques
- Tailwind CSS est déjà configuré (voir `tailwind.config.cjs` et `src/index.css`).
- Pour arrêter le serveur dev : appuyer sur **Ctrl+C** dans le terminal.

---

je suis gael
